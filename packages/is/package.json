{
  "name": "for-demo-package-is",
  "version": "0.1.0",
  "scripts": {
    "package:clean": "rm -rf ./dist",
    "build:packages": "tsc -p tsconfig.build.json",
    "lint": "tslint -p tsconfig.json",
    "test": "jest"
  },
  "files": [
    "dist/**/*"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "slugify": "^1.4.0"
  }
}
